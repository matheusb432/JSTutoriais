let selectedColors = ['red', 'blue'];  // Declarando um Array, tambem chamado de Array Literal
selectedColors[2] = 'green';  // O tamanho do array eh dinamico, entao pode declarar um elemento a mais normalmente assim.
selectedColors[3] = 10;  // Os tipos de elementos do array tambem sao dinamicos, entao pode guardar strings/booleans e numbers igualmente.
// Em JS, o Array eh um objeto.
// O array eh uma estrutura de dados que é usado para representar uma lista de itens.

console.log(selectedColors[0], selectedColors[1], selectedColors[2], selectedColors[3]);  // Chamando os elementos especificos de um array eh igual ao C ou Python.