// Sintaxe de uma função em JS => function nome(parametros) {  }
function greet(name, lastName){
    console.log('Hello ' + name + ' ' + lastName);
}

let name = 'Bingus';
let lastName = 'Chabingus';
greet(name, lastName);  // Chamando a funcao com nome(argumentos);
